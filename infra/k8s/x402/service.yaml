apiVersion: v1
kind: Service
metadata:
  name: x402-api
  namespace: x402
  labels: { app: x402-api }
spec:
  type: ClusterIP
  selector: { app: x402-api }
  ports:
    - { name: http, port: 80, targetPort: http, protocol: TCP }
